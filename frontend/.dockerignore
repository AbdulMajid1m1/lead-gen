# Keeping the build context small is the cheapest build-time win available:
# node_modules alone is ~300MB and is reinstalled inside the image anyway.
node_modules
dist
.git
.env
.env.*
*.log
coverage
.DS_Store
shot.mjs
